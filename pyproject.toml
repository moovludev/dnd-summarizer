[project]
name = "dnd-summarizer"
version = "0.1.0"
description = "A CLI program to transcribe a DND session and summarize it"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.86.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.0",
]
